[package]
name = "audio-kernel"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = [
    "rlib",
    "cdylib"
]

[features]
default = []

[target.'cfg(target_arch = "wasm32")'.dependencies]
wee_alloc = "0.4"
[dependencies]
libm = "0.2"

[dev-dependencies]
